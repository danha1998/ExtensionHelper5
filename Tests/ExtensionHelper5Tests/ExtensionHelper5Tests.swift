import XCTest
@testable import ExtensionHelper5

final class ExtensionHelper5Tests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(ExtensionHelper5().text, "Hello, World!")
    }
}
